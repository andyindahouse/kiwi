{
    "name": "@kiwi/utils",
    "main": "dist/index.js",
    "scripts": {
        "build": "rimraf dist && tsc --outDir dist"
    },
    "devDependencies": {
        "rimraf": "^3.0.2",
        "typescript": "4.3.4"
    }
}
